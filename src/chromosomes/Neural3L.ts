import {Chromosome} from "./Chromosome"

export class Neural3L implements Chromosome{

  setInput(input:any, index:number){

  };

  process(){
    var output = new Uint8Array(100);

    return output.buffer;
  };


}
